---
'@helixui/library': patch
---

fix(hx-breadcrumb): consolidate @internal jsdoc blocks so cem correctly excludes private members; add missing @cssprop for focus ring color; remove invalid role="list" from default story
