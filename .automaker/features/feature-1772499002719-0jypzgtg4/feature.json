{
  "category": "Authority Ideas",
  "description": "Implement Next.js 15 Route Handlers for CRUD operations on the library registry, consuming the registry-store utilities from Phase 1. Create two route files: (1) apps/admin/src/app/api/libraries/route.ts — GET returns all entries as JSON array; POST accepts a LibraryEntry body, runs validateEntry, calls addEntry, returns 201 with the new entry or 400 with validation errors. (2) apps/admin/src/app/api/libraries/[id]/route.ts — GET returns single entry by id or 404; PATCH accepts partial LibraryEntry, validates merged result, calls updateEntry, returns 200; DELETE calls removeEntry, returns 204, returns 403 if id is 'hx-library'. All routes must return JSON with consistent error shape { error: string, details?: string[] }. Use Next.js 15 'use server' / async function syntax with NextRequest/NextResponse. Add try/catch around all store calls — file I/O errors return 500. No authentication required (dev tool). Do not modify any existing API routes.",
  "title": "Phase 2: REST API Routes for Library Registry",
  "status": "backlog",
  "workItemState": "planned",
  "epicId": "feature-1772498927473-b350n8c7v",
  "projectSlug": "phase-2-universal-web-component-management-health-",
  "milestoneSlug": "m1-library-registry-foundation",
  "complexity": "small",
  "id": "feature-1772499002719-0jypzgtg4",
  "branchName": "feature/phase-2-rest-api-routes-for-library",
  "descriptionHistory": [
    {
      "description": "Implement Next.js 15 Route Handlers for CRUD operations on the library registry, consuming the registry-store utilities from Phase 1. Create two route files: (1) apps/admin/src/app/api/libraries/route.ts — GET returns all entries as JSON array; POST accepts a LibraryEntry body, runs validateEntry, calls addEntry, returns 201 with the new entry or 400 with validation errors. (2) apps/admin/src/app/api/libraries/[id]/route.ts — GET returns single entry by id or 404; PATCH accepts partial LibraryEntry, validates merged result, calls updateEntry, returns 200; DELETE calls removeEntry, returns 204, returns 403 if id is 'hx-library'. All routes must return JSON with consistent error shape { error: string, details?: string[] }. Use Next.js 15 'use server' / async function syntax with NextRequest/NextResponse. Add try/catch around all store calls — file I/O errors return 500. No authentication required (dev tool). Do not modify any existing API routes.",
      "timestamp": "2026-03-03T00:50:02.719Z",
      "source": "initial"
    }
  ],
  "createdAt": "2026-03-03T00:50:02.719Z",
  "statusHistory": [
    {
      "from": null,
      "to": "backlog",
      "timestamp": "2026-03-03T00:50:02.719Z",
      "reason": "Feature created"
    }
  ]
}