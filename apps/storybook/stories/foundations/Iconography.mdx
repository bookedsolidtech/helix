{/* Iconography.mdx — Foundations / Iconography */}

import { Meta } from '@storybook/addon-docs/blocks';
import { EyebrowHeading, SectionHead, DocsCard, StatCard, CodeBlock, CodeTabs } from '../_components';

<Meta
  title="Foundations/Iconography"
  parameters={{ controls: { disable: true }, actions: { disable: true } }}
/>

export const SIZES = [
  { name: '12', px: 12, token: '--hx-size-3' },
  { name: '16', px: 16, token: '--hx-size-4' },
  { name: '20', px: 20, token: '--hx-size-5' },
  { name: '24', px: 24, token: '--hx-size-6' },
  { name: '32', px: 32, token: '--hx-size-8' },
  { name: '40', px: 40, token: '--hx-size-10' },
];

export function Glyph({ d, size = 24 }) {
  return (
    <svg
      width={size}
      height={size}
      viewBox="0 0 24 24"
      fill="none"
      stroke="currentColor"
      strokeWidth={1.75}
      strokeLinecap="round"
      strokeLinejoin="round"
      aria-hidden="true"
    >
      {d}
    </svg>
  );
}

export const PLUS = <path d="M12 5v14M5 12h14" />;
export const CHECK = <path d="M5 12l4 4L19 7" />;
export const CLOSE = <path d="M6 6l12 12M18 6L6 18" />;
export const CHEVRON_DOWN = <path d="M6 9l6 6 6-6" />;
export const ALERT = (
  <>
    <circle cx="12" cy="12" r="9" />
    <path d="M12 7v6M12 17h.01" />
  </>
);
export const INFO = (
  <>
    <circle cx="12" cy="12" r="9" />
    <path d="M12 8h.01M11 12h1v5h1" />
  </>
);
export const WARNING = (
  <>
    <path d="M12 3l10 18H2z" />
    <path d="M12 9v5M12 18h.01" />
  </>
);
export const SUCCESS = (
  <>
    <circle cx="12" cy="12" r="9" />
    <path d="M8 12l3 3 5-6" />
  </>
);
export const SEARCH = (
  <>
    <circle cx="11" cy="11" r="6" />
    <path d="M21 21l-4.35-4.35" />
  </>
);
export const HEART = (
  <path d="M12 21s-7-4.35-7-10a4 4 0 0 1 7-2.6A4 4 0 0 1 19 11c0 5.65-7 10-7 10z" />
);
export const CALENDAR = (
  <>
    <rect x="3" y="5" width="18" height="16" rx="2" />
    <path d="M16 3v4M8 3v4M3 11h18" />
  </>
);

<div className="hx-docs">

<EyebrowHeading
  eyebrow="Iconography"
  title="A registry, not an icon set."
  lede={
    <>
      <code>hx-icon</code> resolves names through registered libraries — the same pattern Shoelace
      established and that the WC ecosystem has converged on. <strong>FA Free</strong> is the
      out-of-the-box default. <strong>helix</strong> is the curated internal glyph set powering every
      checkmark, chevron, and status indicator across the library. <strong>Bring your own</strong>{' '}
      — Font Awesome Pro, Phosphor, Heroicons, Material Symbols, Iconify, or a brand-internal set —
      via a one-call registration. Powered by <code>@helixui/icons</code>, a peer dependency
      required like <code>@helixui/tokens</code>.
    </>
  }
/>

<section
  style={{
    background: 'var(--hx-color-info-50, var(--hx-color-surface-raised))',
    border: '1px solid var(--hx-color-info-300, var(--hx-color-border-default))',
    borderRadius: 12,
    padding: '20px 24px',
    margin: '32px 0',
  }}
>
  <strong style={{ display: 'block', marginBottom: 6 }}>Status — landing in 3.9.0</strong>
  The registry pattern, <code>library</code> attribute, and <code>@helixui/icons</code> package are
  shipping on the <code>feat/helixui-icons</code> epic branch. This page documents the architectural
  shape and the API consumers will write against. Until 3.9.0 ships,{' '}
  <code>hx-icon</code>'s existing <code>name</code>+<code>sprite-url</code> and <code>src</code>{' '}
  modes remain the production path — both stay supported as escape hatches after 3.9.0.
</section>

<section>
  <SectionHead title="At a glance" meta="three libraries, one resolution path" />
  <div
    style={{
      display: 'grid',
      gridTemplateColumns: 'repeat(auto-fit, minmax(220px, 1fr))',
      gap: 16,
    }}
  >
    <StatCard
      label="Default library"
      value="fa-free"
      sublabel="~1,900 FA Free Solid v7.x"
      tone="info"
    />
    <StatCard
      label="System library"
      value="helix"
      sublabel="~30 curated internal glyphs"
      tone="success"
    />
    <StatCard
      label="Bring-your-own"
      value="∞"
      sublabel="register any resolver"
      tone="neutral"
    />
    <StatCard
      label="Conformance"
      value="AAA"
      sublabel="non-text contrast 1.4.11"
      tone="success"
    />
  </div>
</section>

<section>
  <SectionHead title="The shape" meta="hx-icon library=… name=…" />
  <CodeBlock
    language="html"
    code={`<!-- Default library — no attribute needed -->
<hx-icon name="heart" hx-size="md"></hx-icon>

<!-- Explicit FA Free -->
<hx-icon library="fa-free" name="user-md" hx-size="md"></hx-icon>

<!-- helix system glyph -->
<hx-icon library="helix" name="check" hx-size="sm"></hx-icon>

<!-- Brand-registered library -->
<hx-icon library="acme-medical" name="vitals" hx-size="lg"></hx-icon>`}
  />
</section>

<section>
  <SectionHead title="Built-in libraries" meta="auto-registered by @helixui/icons" />

  <DocsCard
    title="fa-free — the default"
    tag="Font Awesome Free Solid · CC BY 4.0"
    demo={
      <div style={{ display: 'flex', gap: 24, alignItems: 'center', flexWrap: 'wrap' }}>
        <Glyph d={HEART} size={28} />
        <Glyph d={SEARCH} size={28} />
        <Glyph d={CALENDAR} size={28} />
        <Glyph d={PLUS} size={28} />
      </div>
    }
  >
    Roughly 1,900 glyphs from FA Free Solid v7. Pre-built sprite served from{' '}
    <code>cdn.jsdelivr.net/npm/@helixui/icons/dist/fa-free-solid.svg</code> by default — one HTTP
    request, browser-cached forever. Self-host the sprite via{' '}
    <code>setBasePath('/your-assets')</code> for Drupal, locked-down enterprise networks, or strict
    CSP environments. Tree-shakeable per-icon ESM imports also available for SPA consumers who
    prefer build-time over CDN.
  </DocsCard>

  <DocsCard
    title="helix — the system"
    tag="curated internal glyphs · MIT"
    demo={
      <div style={{ display: 'flex', gap: 24, alignItems: 'center', flexWrap: 'wrap' }}>
        <Glyph d={CHECK} size={28} />
        <Glyph d={CLOSE} size={28} />
        <Glyph d={CHEVRON_DOWN} size={28} />
        <Glyph d={INFO} size={28} />
        <Glyph d={WARNING} size={28} />
        <Glyph d={SUCCESS} size={28} />
        <Glyph d={ALERT} size={28} />
      </div>
    }
  >
    The internal glyph vocabulary every HELiX component renders through —{' '}
    <code>check</code>, <code>dash</code>, <code>close</code>, <code>chevron-up/down/left/right</code>,
    <code>info</code>, <code>warning</code>, <code>error</code>, <code>success</code>,{' '}
    <code>star-filled/outline</code>, <code>copy</code>, <code>upload</code>, <code>file</code>, and
    the navigation/control indicators. Hand-drawn at 24×24, stroked at 2px or solid path,{' '}
    <code>currentColor</code> only. AAA-validated for WCAG 1.4.11 non-text contrast at 16px+ render
    sizes.
  </DocsCard>
</section>

<section>
  <SectionHead title="Bring your own library" meta="registerIconLibrary()" />
  <p style={{ marginBottom: 16, maxWidth: '70ch' }}>
    The same wire-compatible registry pattern Shoelace established. Register a resolver once at app
    bootstrap; every <code>{'<hx-icon library="…">'}</code> call resolves through it. Optional{' '}
    <code>mutator</code> hook lets you enforce <code>fill="currentColor"</code> or strip stroke
    widths for AAA cert needs. Optional <code>spriteSheet: true</code> switches the resolver to
    sprite-href mode for SSR-friendly rendering.
  </p>

  <CodeTabs
    tabs={[
      {
        label: 'FA Pro',
        language: 'ts',
        code: `// Font Awesome Pro Medical (paid tier — your license, your assets)
import { registerIconLibrary } from '@helixui/icons';

registerIconLibrary('fa-pro', {
  resolver: (name) => \`/assets/fa-pro/svgs/solid/\${name}.svg\`,
});

// Use:
// <hx-icon library="fa-pro" name="stethoscope"></hx-icon>`,
      },
      {
        label: 'Iconify',
        language: 'ts',
        code: `// Iconify — 200,000+ icons across 150+ sets via API
import { registerIconLibrary } from '@helixui/icons';

registerIconLibrary('iconify', {
  resolver: (name) => \`https://api.iconify.design/\${name}.svg\`,
  mutator: (svg) => svg.setAttribute('fill', 'currentColor'),
});

// Use:
// <hx-icon library="iconify" name="mdi:account-heart"></hx-icon>`,
      },
      {
        label: 'Phosphor',
        language: 'ts',
        code: `// Phosphor — 9,000+ icons across 6 weights
import { registerIconLibrary } from '@helixui/icons';

registerIconLibrary('phosphor', {
  resolver: (name) =>
    \`https://unpkg.com/@phosphor-icons/core/assets/regular/\${name}.svg\`,
});

// Use:
// <hx-icon library="phosphor" name="heart"></hx-icon>`,
      },
      {
        label: 'Brand sprite',
        language: 'ts',
        code: `// Self-hosted brand sprite — one fetch, all icons cached
import { registerIconLibrary } from '@helixui/icons';

registerIconLibrary('acme-medical', {
  resolver: (name) => \`/assets/acme-medical-sprite.svg#\${name}\`,
  spriteSheet: true,
});

// Use:
// <hx-icon library="acme-medical" name="vitals"></hx-icon>`,
      },
    ]}
  />
</section>

<section>
  <SectionHead title="Sizes" meta="--hx-icon-size · --hx-size-{3…10}" />
  <div
    style={{
      display: 'flex',
      gap: 16,
      alignItems: 'flex-end',
      flexWrap: 'wrap',
      padding: 24,
      background: 'var(--hx-color-surface-raised)',
      border: '1px solid var(--hx-color-border-default)',
      borderRadius: 12,
    }}
  >
    {SIZES.map((s) => (
      <div
        key={s.name}
        style={{ display: 'flex', flexDirection: 'column', gap: 8, alignItems: 'center' }}
      >
        <Glyph d={CHECK} size={s.px} />
        <div
          style={{
            fontFamily: 'var(--hx-font-family-mono)',
            fontSize: 11,
            color: 'var(--hx-color-text-muted)',
            textAlign: 'center',
          }}
        >
          {s.name}px
          <br />
          {s.token}
        </div>
      </div>
    ))}
  </div>
</section>

<section>
  <SectionHead title="Pairing with text" meta="--hx-icon-color: currentColor" />
  <div
    style={{
      display: 'grid',
      gridTemplateColumns: 'repeat(auto-fit, minmax(280px, 1fr))',
      gap: 16,
    }}
  >
    <DocsCard
      title="Inline with body text"
      tag="hx-icon + p"
      demo={
        <span style={{ display: 'inline-flex', alignItems: 'center', gap: 8 }}>
          <Glyph d={CHECK} size={16} />
          <span>Records uploaded successfully</span>
        </span>
      }
    >
      Icon picks up <code>currentColor</code> by default — pair semantically with{' '}
      <code>--hx-color-text-success</code> or <code>--hx-color-text-error</code> to signal status.
    </DocsCard>
    <DocsCard
      title="Leading icon in a control"
      tag="hx-button + hx-icon"
      demo={
        <button
          style={{
            appearance: 'none',
            border: 0,
            cursor: 'pointer',
            display: 'inline-flex',
            alignItems: 'center',
            gap: 8,
            padding: '0 16px',
            minHeight: 44,
            borderRadius: 'var(--hx-border-radius-md, 6px)',
            background: 'var(--hx-color-action-primary-bg, #0F7078)',
            color: '#fff',
            fontWeight: 500,
            font: 'inherit',
          }}
        >
          <Glyph d={PLUS} size={16} />
          Add patient
        </button>
      }
    >
      Always pair an icon-only control with an <code>aria-label</code>; for text+icon controls keep
      the icon decorative so the label is not announced twice.
    </DocsCard>
  </div>
</section>

<section>
  <SectionHead title="Accessibility" meta="WCAG 2.2 AAA · forced-colors · 1.4.11" />
  <div
    style={{
      display: 'grid',
      gridTemplateColumns: 'repeat(auto-fit, minmax(280px, 1fr))',
      gap: 16,
    }}
  >
    <DocsCard
      title="Decorative by default"
      tag="aria-hidden"
      demo={
        <code style={{ fontSize: 13 }}>{'<hx-icon name="check"></hx-icon>'}</code>
      }
    >
      With no <code>label</code>, <code>aria-hidden="true"</code> is applied and the SVG is hidden
      from assistive technology. This is the right default — the surrounding text is the
      accessible name.
    </DocsCard>
    <DocsCard
      title="Labeled when standalone"
      tag="role=img + aria-label"
      demo={
        <code style={{ fontSize: 13 }}>{'<hx-icon name="alert" label="Critical"></hx-icon>'}</code>
      }
    >
      A non-empty <code>label</code> opts into <code>role="img"</code> +{' '}
      <code>aria-label</code> + <code>{'<title>'}</code>. Use only when the icon carries meaning
      that no adjacent text conveys.
    </DocsCard>
    <DocsCard
      title="Forced-colors safe"
      tag="System Colors via currentColor"
      demo={
        <code style={{ fontSize: 13 }}>{'fill: currentColor; stroke: currentColor;'}</code>
      }
    >
      Every glyph in the <code>helix</code> library uses <code>currentColor</code> exclusively. FA
      Free Solid is normalized to <code>currentColor</code> at sprite-build time. High-contrast and
      forced-colors modes flow through the parent's text token automatically.
    </DocsCard>
    <DocsCard
      title="Non-text contrast 1.4.11"
      tag="≥3:1 against background"
      demo={
        <code style={{ fontSize: 13 }}>{'iconLibraryAaaVerdict("helix")'}</code>
      }
    >
      Both built-in libraries are validated against WCAG 2.2 1.4.11 at minimum render size. Custom
      libraries inherit the same harness — call{' '}
      <code>iconLibraryAaaVerdict(name)</code> to verify before claiming AAA on icon glyphs.
    </DocsCard>
  </div>
</section>

<section>
  <SectionHead title="Escape hatches" meta="when the registry isn't right" />
  <div
    style={{
      display: 'grid',
      gridTemplateColumns: 'repeat(auto-fit, minmax(320px, 1fr))',
      gap: 16,
    }}
  >
    <DocsCard
      title="Explicit sprite URL"
      tag="sprite-url + name"
      demo={
        <code style={{ fontSize: 13 }}>
          {'<hx-icon name="logo" sprite-url="/brand.svg"></hx-icon>'}
        </code>
      }
    >
      Skip registry entirely — point at a self-hosted sprite, render{' '}
      <code>{'<svg><use href="/brand.svg#logo">'}</code>. Useful for single-page brand assets where
      registering a library is overkill.
    </DocsCard>
    <DocsCard
      title="Inline fetch + sanitize"
      tag="src=URL"
      demo={
        <code style={{ fontSize: 13 }}>{'<hx-icon src="/icons/foo.svg"></hx-icon>'}</code>
      }
    >
      Fetch + DOMParser sanitize + inject. Strips <code>{'<script>'}</code>,{' '}
      <code>{'<foreignObject>'}</code>, SMIL animations, <code>on*</code> handlers,{' '}
      <code>javascript:</code>/<code>data:</code> URIs. Same-origin by default; explicit allowlist
      via <code>allowed-origins</code>.
    </DocsCard>
  </div>
</section>

<section>
  <SectionHead title="Don't" meta="anti-patterns" />
  <div
    style={{
      display: 'grid',
      gridTemplateColumns: 'repeat(auto-fit, minmax(260px, 1fr))',
      gap: 16,
    }}
  >
    <div className="hx-docs-card" style={{ borderColor: 'var(--hx-color-error-500)' }}>
      <div className="hx-docs-card-head">
        <h4 className="hx-docs-card-title">Don't bundle FA Pro</h4>
        <span className="hx-docs-card-tag">license + bundle</span>
      </div>
      <p className="hx-docs-card-body">
        Register a resolver pointing at your own CDN or static assets — never inline FA Pro SVGs
        into your application bundle. The license requires their distribution to flow from you, not
        through us.
      </p>
    </div>
    <div className="hx-docs-card" style={{ borderColor: 'var(--hx-color-error-500)' }}>
      <div className="hx-docs-card-head">
        <h4 className="hx-docs-card-title">Don't scale below 12px</h4>
        <span className="hx-docs-card-tag">stroke breaks</span>
      </div>
      <p className="hx-docs-card-body">
        Below 12px, 2px strokes alias against pixel boundaries and 1.4.11 non-text contrast becomes
        unreliable. Use <code>--hx-size-3</code> as the floor.
      </p>
    </div>
    <div className="hx-docs-card" style={{ borderColor: 'var(--hx-color-error-500)' }}>
      <div className="hx-docs-card-head">
        <h4 className="hx-docs-card-title">Don't pair filled with stroked</h4>
        <span className="hx-docs-card-tag">visual weight</span>
      </div>
      <p className="hx-docs-card-body">
        Mixing fill and stroke styles in the same composition reads as inconsistency. Pick one
        family per surface — <code>helix</code> + FA Free Solid mix cleanly because both are
        solid-fill at the sprite layer.
      </p>
    </div>
    <div className="hx-docs-card" style={{ borderColor: 'var(--hx-color-error-500)' }}>
      <div className="hx-docs-card-head">
        <h4 className="hx-docs-card-title">Don't hardcode color</h4>
        <span className="hx-docs-card-tag">forced-colors</span>
      </div>
      <p className="hx-docs-card-body">
        Always set <code>--hx-icon-color</code> via a token reference. Hardcoded fills break
        forced-colors mode and prevent <code>currentColor</code> inheritance from the parent text
        token.
      </p>
    </div>
  </div>
</section>

</div>
