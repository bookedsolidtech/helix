{/* Iconography.mdx — Foundations / Iconography */}

import { Meta } from '@storybook/addon-docs/blocks';
import { EyebrowHeading, SectionHead, DocsCard } from '../_components';

<Meta
  title="Foundations/Iconography"
  parameters={{ controls: { disable: true }, actions: { disable: true } }}
/>

export const SIZES = [
  { name: '12', px: 12, token: '--hx-size-3' },
  { name: '16', px: 16, token: '--hx-size-4' },
  { name: '20', px: 20, token: '--hx-size-5' },
  { name: '24', px: 24, token: '--hx-size-6' },
  { name: '32', px: 32, token: '--hx-size-8' },
  { name: '40', px: 40, token: '--hx-size-10' },
];

export function Glyph({ d, size = 24 }) {
  return (
    <svg
      width={size}
      height={size}
      viewBox="0 0 24 24"
      fill="none"
      stroke="currentColor"
      strokeWidth={1.75}
      strokeLinecap="round"
      strokeLinejoin="round"
      aria-hidden="true"
    >
      {d}
    </svg>
  );
}

export const PLUS = <path d="M12 5v14M5 12h14" />;
export const CHECK = <path d="M5 12l4 4L19 7" />;
export const ALERT = (
  <>
    <circle cx="12" cy="12" r="9" />
    <path d="M12 7v6M12 17h.01" />
  </>
);
export const SEARCH = (
  <>
    <circle cx="11" cy="11" r="6" />
    <path d="M21 21l-4.35-4.35" />
  </>
);
export const HEART = (
  <path d="M12 21s-7-4.35-7-10a4 4 0 0 1 7-2.6A4 4 0 0 1 19 11c0 5.65-7 10-7 10z" />
);
export const CALENDAR = (
  <>
    <rect x="3" y="5" width="18" height="16" rx="2" />
    <path d="M16 3v4M8 3v4M3 11h18" />
  </>
);

<div className="hx-docs">

<EyebrowHeading
  eyebrow="Iconography"
  title="Geometric. Stroke-aligned. Pixel-snapped."
  lede={
    <>
      Icons live in the <code>hx-icon</code> host. Sprite mode is the production default — one HTTP
      fetch, declarative <code>{'<use href="#…">'}</code>, server-renderable in Drupal and Twig.
      Inline mode is for one-off marks at the application level. Stroke-aligned to a 24×24 grid;
      never scaled below 12px.
    </>
  }
/>

<section>
  <SectionHead title="Sample glyphs" meta="stroke 1.75 · cap round · join round" />
  <div
    style={{
      display: 'grid',
      gridTemplateColumns: 'repeat(auto-fit, minmax(120px, 1fr))',
      gap: 16,
    }}
  >
    {[
      { label: 'plus', d: PLUS },
      { label: 'check', d: CHECK },
      { label: 'alert', d: ALERT },
      { label: 'search', d: SEARCH },
      { label: 'heart', d: HEART },
      { label: 'calendar', d: CALENDAR },
    ].map((g) => (
      <div
        key={g.label}
        style={{
          display: 'flex',
          flexDirection: 'column',
          alignItems: 'center',
          gap: 8,
          padding: 18,
          background: 'var(--hx-color-surface-raised)',
          border: '1px solid var(--hx-color-border-default)',
          borderRadius: 12,
          color: 'var(--hx-color-text-primary)',
        }}
      >
        <Glyph d={g.d} size={32} />
        <span
          style={{
            fontFamily: 'var(--hx-font-family-mono)',
            fontSize: 11,
            color: 'var(--hx-color-text-muted)',
          }}
        >
          {g.label}
        </span>
      </div>
    ))}
  </div>
</section>

<section>
  <SectionHead title="Sizes" meta="--hx-icon-size · --hx-size-{3…10}" />
  <div
    style={{
      display: 'flex',
      gap: 16,
      alignItems: 'flex-end',
      flexWrap: 'wrap',
      padding: 24,
      background: 'var(--hx-color-surface-raised)',
      border: '1px solid var(--hx-color-border-default)',
      borderRadius: 12,
    }}
  >
    {SIZES.map((s) => (
      <div
        key={s.name}
        style={{ display: 'flex', flexDirection: 'column', gap: 8, alignItems: 'center' }}
      >
        <Glyph d={CHECK} size={s.px} />
        <div
          style={{
            fontFamily: 'var(--hx-font-family-mono)',
            fontSize: 11,
            color: 'var(--hx-color-text-muted)',
            textAlign: 'center',
          }}
        >
          {s.name}px
          <br />
          {s.token}
        </div>
      </div>
    ))}
  </div>
</section>

<section>
  <SectionHead title="Pairing with text" meta="--hx-icon-color: currentColor" />
  <div
    style={{
      display: 'grid',
      gridTemplateColumns: 'repeat(auto-fit, minmax(280px, 1fr))',
      gap: 16,
    }}
  >
    <DocsCard
      title="Inline with body text"
      tag="hx-icon + p"
      demo={
        <span style={{ display: 'inline-flex', alignItems: 'center', gap: 8 }}>
          <Glyph d={CHECK} size={16} />
          <span>Records uploaded successfully</span>
        </span>
      }
    >
      Icon picks up <code>currentColor</code> by default — pair semantically with{' '}
      <code>--hx-color-text-success</code> or <code>--hx-color-text-error</code> to signal status.
    </DocsCard>
    <DocsCard
      title="Leading icon in a control"
      tag="hx-button + svg"
      demo={
        <button
          style={{
            appearance: 'none',
            border: 0,
            cursor: 'pointer',
            display: 'inline-flex',
            alignItems: 'center',
            gap: 8,
            padding: '0 16px',
            minHeight: 44,
            borderRadius: 'var(--hx-border-radius-md, 6px)',
            background: 'var(--hx-color-action-primary-bg, #0F7078)',
            color: '#fff',
            fontWeight: 500,
            font: 'inherit',
          }}
        >
          <Glyph d={PLUS} size={16} />
          Add patient
        </button>
      }
    >
      Always pair an icon-only control with an <code>aria-label</code>; pair text+icon controls by
      hiding the SVG via <code>aria-hidden="true"</code> so the label is not announced twice.
    </DocsCard>
  </div>
</section>

<section>
  <SectionHead title="Don't" meta="anti-patterns" />
  <div
    style={{
      display: 'grid',
      gridTemplateColumns: 'repeat(auto-fit, minmax(260px, 1fr))',
      gap: 16,
    }}
  >
    <div className="hx-docs-card" style={{ borderColor: 'var(--hx-color-error-500)' }}>
      <div className="hx-docs-card-head">
        <h4 className="hx-docs-card-title">Don't scale below 12px</h4>
        <span className="hx-docs-card-tag">stroke breaks</span>
      </div>
      <p className="hx-docs-card-body">
        Below 12px, the 1.75 stroke begins to alias against pixel boundaries. Use{' '}
        <code>--hx-size-3</code> as the floor.
      </p>
    </div>
    <div className="hx-docs-card" style={{ borderColor: 'var(--hx-color-error-500)' }}>
      <div className="hx-docs-card-head">
        <h4 className="hx-docs-card-title">Don't pair filled with stroked</h4>
        <span className="hx-docs-card-tag">visual weight</span>
      </div>
      <p className="hx-docs-card-body">
        Mixing fill and stroke styles in the same composition reads as inconsistency. Pick one
        family per surface.
      </p>
    </div>
    <div className="hx-docs-card" style={{ borderColor: 'var(--hx-color-error-500)' }}>
      <div className="hx-docs-card-head">
        <h4 className="hx-docs-card-title">Don't override color hard-coded</h4>
        <span className="hx-docs-card-tag">forced-colors</span>
      </div>
      <p className="hx-docs-card-body">
        Always set <code>--hx-icon-color</code> via a token reference so high-contrast and forced
        colors flow through the cascade.
      </p>
    </div>
  </div>
</section>

</div>
