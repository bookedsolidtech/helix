{/* Spacing.mdx — Foundations / Spacing & density (replaces tokens/Spacing.mdx) */}

import { Meta } from '@storybook/addon-docs/blocks';
import { EyebrowHeading, SectionHead } from '../_components';

<Meta
  title="Foundations/Spacing"
  parameters={{ controls: { disable: true }, actions: { disable: true } }}
/>

export const SCALE = [
  { token: '0', px: 0 },
  { token: 'px', px: 1 },
  { token: '0-5', px: 2 },
  { token: '1', px: 4 },
  { token: '1-5', px: 6 },
  { token: '2', px: 8 },
  { token: '3', px: 12 },
  { token: '4', px: 16 },
  { token: '5', px: 20 },
  { token: '6', px: 24 },
  { token: '7', px: 28 },
  { token: '8', px: 32 },
  { token: '10', px: 40 },
  { token: '12', px: 48 },
  { token: '14', px: 56 },
  { token: '16', px: 64 },
  { token: '20', px: 80 },
  { token: '24', px: 96 },
  { token: '32', px: 128 },
  { token: '40', px: 160 },
  { token: '48', px: 192 },
  { token: '64', px: 256 },
];

<div className="hx-docs">

<EyebrowHeading
  eyebrow="Spacing & surface"
  title="Rhythm. Radius. Elevation."
  lede={
    <>
      One 4&nbsp;px base, with finer 1&nbsp;px / 2&nbsp;px / 6&nbsp;px stops for tight UI and
      coarser 32 → 256&nbsp;px stops for layout-scale spacing. The same scale powers grid gaps,
      component padding, and the three <code>hx-theme</code> density modes —{' '}
      <code>comfortable</code> (default), <code>compact</code>, and <code>spacious</code>.
    </>
  }
/>

<section>
  <SectionHead title="The scale" meta="--hx-space-{0 · px · 0-5 · 1 · … · 64}" />
  <div style={{ display: 'flex', flexDirection: 'column', gap: 4 }}>
    {SCALE.map((s) => (
      <div
        key={s.token}
        style={{
          display: 'grid',
          gridTemplateColumns: '120px 80px 1fr',
          gap: 16,
          alignItems: 'center',
          padding: '12px 0',
          borderBottom: '1px dashed var(--hx-color-border-subtle)',
        }}
      >
        <div
          style={{
            fontFamily: 'var(--hx-font-family-mono)',
            fontSize: 12,
            color: 'var(--hx-color-text-primary)',
          }}
        >
          space-{s.token}
        </div>
        <div
          style={{
            fontFamily: 'var(--hx-font-family-mono)',
            fontSize: 11,
            color: 'var(--hx-color-text-muted)',
          }}
        >
          {s.px}px
        </div>
        <div
          style={{
            background: 'var(--hx-color-primary-200)',
            borderRadius: 4,
            height: 24,
            width: `var(--hx-space-${s.token}, ${s.px}px)`,
            minWidth: s.px === 0 ? 1 : undefined,
          }}
        />
      </div>
    ))}
  </div>
</section>

<section>
  <SectionHead title="Density modes" meta="same component · three space-token scales" />
  <p className="hx-docs-card-body">
    A patient list rendered at three densities. The markup is identical — what changes is the
    underlying <code>--hx-space-*</code> token scale (rescaled ~25% down for <code>compact</code>{' '}
    and ~25% up for <code>spacious</code>). Because density rescales spacing rather than touching
    individual rules, padding, gap, input height, and on the compact end typography all move
    together. The demonstration tiles below illustrate the resulting padding deltas; the literal
    pixel values shown are demo numbers, not the canonical tokens — set <code>density</code> on{' '}
    <code>hx-theme</code> to apply the real cascade.
  </p>
  <div
    style={{
      display: 'grid',
      gridTemplateColumns: 'repeat(auto-fit, minmax(280px, 1fr))',
      gap: 16,
      marginTop: 16,
    }}
  >
    {[
      { name: 'Compact', toks: '~25% smaller spacing tokens', pad: 8, gap: 4 },
      { name: 'Comfortable (default)', toks: 'baseline spacing tokens', pad: 12, gap: 8 },
      { name: 'Spacious', toks: '~25% larger spacing tokens', pad: 20, gap: 16 },
    ].map((mode) => (
      <div key={mode.name} className="hx-docs-card">
        <div className="hx-docs-card-head">
          <h4 className="hx-docs-card-title">{mode.name}</h4>
          <span className="hx-docs-card-tag">{mode.toks}</span>
        </div>
        <ul
          style={{
            listStyle: 'none',
            margin: 0,
            padding: 0,
            display: 'flex',
            flexDirection: 'column',
            gap: mode.gap,
          }}
        >
          {['Vega, Annika · 41', 'Tran, Mei · 67', 'Ojo, Femi · 22', 'Schwartz, Eli · 58'].map(
            (p) => (
              <li
                key={p}
                style={{
                  padding: `${mode.pad}px 12px`,
                  background: 'var(--hx-color-surface-default)',
                  border: '1px solid var(--hx-color-border-subtle)',
                  borderRadius: 6,
                  fontSize: 13,
                  color: 'var(--hx-color-text-primary)',
                }}
              >
                {p}
              </li>
            ),
          )}
        </ul>
      </div>
    ))}
  </div>
</section>

<section>
  <SectionHead title="Touch target floor" meta="WCAG 2.5.5 — 44px Enhanced AAA" />
  <p className="hx-docs-card-body">
    Every interactive host (<code>hx-button</code>, <code>hx-checkbox</code>, <code>hx-switch</code>
    ,<code> hx-tabs</code>, …) honors <code>--hx-touch-target-min</code> (default{' '}
    <code>2.75rem</code> /<code> 44px</code>) on every variant — <code>compact</code> density
    rescales the spacing tokens but does not lower the touch-target floor. WCAG 2.5.5 Target Size
    (Enhanced AAA) is a P0 cert criterion, so the 44px floor holds across every shipped density
    mode.
  </p>
</section>

</div>
