{/* Color.mdx — Foundations / Color (enterprise redesign — cascade story + AAA matrix + click-to-copy) */}

import { Meta } from '@storybook/addon-docs/blocks';
import {
  EyebrowHeading,
  SectionHead,
  TokenSwatchGrid,
  SurfaceCard,
  TokenRef,
  ContrastMatrix,
} from '../_components';

// Side-effect imports register the custom elements used inline below
// (`hx-button` in the Cascade walkthrough, `hx-alert` in the four Status
// cards). MDX has no global element registration — every page imports the
// components it renders, mirroring Dashboard.mdx / ForcedColors.mdx.
import '@helixui/library/components/hx-button';
import '@helixui/library/components/hx-alert';

<Meta
  title="Foundations/Color"
  parameters={{ controls: { disable: true }, actions: { disable: true } }}
/>

<div className="hx-docs">

<EyebrowHeading
  eyebrow="Color · system"
  title="Three tiers, three modes, every pair AAA-graded."
  lede={
    <>
      HELiX color is a three-tier cascade — primitive ramps anchor the system, semantic surfaces
      consume the ramps, and components consume the semantics. The <strong>semantic tier</strong> is
      the stable override point for consumers; primitive ramps are only overridden by registered
      brand definitions (primary/secondary), and component-tier hooks may be internal,
      variant-shadowed, or only effective in limited states. Every text-on-surface pair is
      contrast-graded across light, dark, and high-contrast modes. Click any token reference on
      this page to copy <code>var(--hx-color-…)</code>.
    </>
  }
/>

<section>
  <SectionHead title="Cascade walkthrough" meta="primitive → semantic → component" />
  <p className="hx-docs-card-body">
    One concrete example, top to bottom. The primitive ramp anchors the hue; the semantic layer
    binds the ramp stop to a role consumers override; the component reads the semantic role.
    Override at the semantic tier — primitives stay locked to the ramp, components stay locked
    to the role.
  </p>

  <div className="hx-docs-cascade" style={{ marginTop: 24 }}>
    <div className="hx-docs-cascade-stages">
      <div className="hx-docs-cascade-stage" data-tier="primitive">
        <span className="hx-docs-cascade-tier">tier 1 · primitive</span>
        <h4>The raw ramp stop</h4>
        <p>Anchor hue. Versioned and AA-tuned; not overridden for routine theming. Registered brand definitions are the one exception — they override the primary and secondary ramp stops.</p>
        <TokenRef token="--hx-color-primary-600" variant="block" />
      </div>

      <div className="hx-docs-cascade-stage" data-tier="semantic">
        <span className="hx-docs-cascade-tier">tier 2 · semantic</span>
        <h4>The role consumers override</h4>
        <p>Binds the primitive to an action role. Override here for theming.</p>
        <TokenRef token="--hx-color-action-primary-bg" variant="block" />
      </div>

      <div className="hx-docs-cascade-stage" data-tier="component">
        <span className="hx-docs-cascade-tier">tier 3 · component</span>
        <h4>The host element reads the role</h4>
        <p>The variant CSS sets <code>--hx-button-bg</code> from the semantic role; the rendered button then paints from <code>--hx-button-bg</code>. Override the semantic token to retheme; override the component token only when you need to vary one variant in isolation.</p>
        <TokenRef token={`.button--primary { --hx-button-bg: var(--hx-color-action-primary-bg); }\n.button { background-color: var(--hx-button-bg); }`} copyValue="--hx-button-bg" variant="block" />
      </div>
    </div>

    <div className="hx-docs-cascade-result">
      <span className="hx-docs-cascade-result-label">live render</span>
      <hx-button variant="primary">Schedule visit</hx-button>
      <span className="hx-docs-cascade-result-label" style={{ marginTop: 8 }}>
        toggle the Brand toolbar above to see the cascade reflow live
      </span>
    </div>

  </div>
</section>

<section>
  <SectionHead title="Semantic surfaces" meta="--hx-color-surface-{role}" />
  <p className="hx-docs-card-body">
    Every surface ships with at least one paired text token whose contrast is AA at minimum and AAA
    where the cascade allows. The ratio printed on each card is the live computed value against the
    paired text token in the current theme.
  </p>
  <div className="hx-docs-surface-grid" style={{ marginTop: 24 }}>
    <SurfaceCard
      surfaceToken="--hx-color-surface-default"
      pairedWith="--hx-color-text-primary"
      description="Page canvas — body backgrounds, layout shells."
    />
    <SurfaceCard
      surfaceToken="--hx-color-surface-raised"
      pairedWith="--hx-color-text-primary"
      description="Cards, panels, anything elevated above the canvas."
    />
    <SurfaceCard
      surfaceToken="--hx-color-surface-sunken"
      pairedWith="--hx-color-text-primary"
      description="Wells, code blocks, recessed regions."
    />
    <SurfaceCard
      surfaceToken="--hx-color-surface-inverse"
      pairedWith="--hx-color-text-inverse"
      description="Tooltips, dark navigation, inverse callouts."
    />
  </div>
</section>

<section>
  <SectionHead title="Status colors in context" meta="real components, real surfaces" />
  <p className="hx-docs-card-body">
    Status ramps are evaluated where users see them — inside <code>hx-alert</code>. Each card
    shows the live alert (rendered with <code>open</code>), the tonal ramp stops the alert paints
    from, and the matching surface tint. Click any token reference to copy.
  </p>

  <div className="hx-docs-status-grid" style={{ marginTop: 24 }}>
    <div className="hx-docs-status-card" data-status="success">
      <h4>Success</h4>
      <p>Confirmation, completion, positive verification.</p>
      <hx-alert open variant="success">
        <span slot="title">Visit confirmed</span>
        Your appointment is on the calendar.
      </hx-alert>
      <div className="hx-docs-status-tokens">
        <TokenRef token="--hx-color-success-50" />
        <TokenRef token="--hx-color-success-500" />
        <TokenRef token="--hx-color-success-600" />
      </div>
    </div>

    <div className="hx-docs-status-card" data-status="warning">
      <h4>Warning</h4>
      <p>Caution, attention required, soft block.</p>
      <hx-alert open variant="warning">
        <span slot="title">Action needed</span>
        Confirm your contact details before continuing.
      </hx-alert>
      <div className="hx-docs-status-tokens">
        <TokenRef token="--hx-color-warning-50" />
        <TokenRef token="--hx-color-warning-500" />
        <TokenRef token="--hx-color-warning-600" />
      </div>
    </div>

    <div className="hx-docs-status-card" data-status="error">
      <h4>Error</h4>
      <p>Failure, hard block, destructive consequence.</p>
      <hx-alert open variant="error">
        <span slot="title">Could not save</span>
        Please retry — your network connection dropped.
      </hx-alert>
      <div className="hx-docs-status-tokens">
        <TokenRef token="--hx-color-error-50" />
        <TokenRef token="--hx-color-error-500" />
        <TokenRef token="--hx-color-error-600" />
      </div>
    </div>

    <div className="hx-docs-status-card" data-status="info">
      <h4>Info</h4>
      <p>Neutral context, advisory, FYI.</p>
      <hx-alert open variant="info">
        <span slot="title">New protocol</span>
        Updated triage guidance is live for emergency intake.
      </hx-alert>
      <div className="hx-docs-status-tokens">
        <TokenRef token="--hx-color-info-50" />
        <TokenRef token="--hx-color-info-500" />
        <TokenRef token="--hx-color-info-600" />
      </div>
    </div>

  </div>
</section>

<section>
  <SectionHead title="AAA contrast matrix" meta="every text × surface pair · all 3 modes" />
  <p className="hx-docs-card-body">
    Every shipped text-on-surface pair, graded against the WCAG 2.2 contrast thresholds (SC 1.4.6
    carries forward from 2.1): body text 7:1 AAA / 4.5:1 AA, large text 4.5:1 AAA / 3:1 AA, and
    UI / non-text pairs 3:1 (SC 1.4.11). Rendered from{' '}
    <code>@helixui/tokens/contrast-data</code> (the committed contrast report module imported by
    Storybook); regenerate via{' '}
    <code>pnpm --filter=@helixui/tokens run contrast:report</code>. This view is token contrast
    telemetry — the canonical AAA cert authority is{' '}
    <code>packages/hx-library/aaa-verdicts.json</code> backed by the formal audit harness
    (<code>scripts/aaa-formal-audit.mjs</code>).
  </p>
  <div style={{ marginTop: 24 }}>
    <ContrastMatrix />
  </div>
</section>

<section>
  <SectionHead title="Brand swap" meta="primary + secondary ramps · semantic stays locked" />
  <p className="hx-docs-card-body">
    Open the <strong>Brand</strong> toolbar at the top of this page. Selecting a brand overrides
    the primary and secondary ramp stops; every consumer of those ramps reflows, including the
    live <code>hx-button</code> in the cascade walkthrough — which paints from{' '}
    <TokenRef token="--hx-color-action-primary-bg" /> (currently resolving to the brand{' '}
    <code>primary-700</code> stop for AAA contrast on action surfaces). The 22-token brand
    contract is the primary/secondary ramp set; semantic surface, text, and action tokens stay
    inherited and recompute from those ramps, so contrast guarantees survive the swap.
  </p>
  <p className="hx-docs-card-body" style={{ marginTop: 16 }}>
    Full brand contract:{' '}
    <strong>
      <a href="?path=/docs/foundations-brand-registry--docs">Foundations / Brand Registry</a>
    </strong>
    .
  </p>
</section>

<section>
  <SectionHead title="Primitive ramps" meta="--hx-color-{role}-{50…950} · click to copy any stop" />
  <details className="hx-docs-collapsible">
    <summary>
      <span>Open the full 8 × 11 swatch matrix</span>
    </summary>
    <div className="hx-docs-collapsible-body">
      <p className="hx-docs-collapsible-meta">
        Eight color roles, each with a precision-cool 50&nbsp;→&nbsp;950 ramp. Stops 500–700 are
        the interactive surfaces; 50–200 are the tints; 800–950 are dark-mode pages and emphasis
        fills. Every stop is click-to-copy.
      </p>

      <div>
        <h4 className="hx-docs-card-title" style={{ marginBottom: 12 }}>
          Brand ramps
        </h4>
        <TokenSwatchGrid ramp="primary" />
        <TokenSwatchGrid ramp="secondary" />
        <TokenSwatchGrid ramp="accent" />
      </div>

      <div>
        <h4 className="hx-docs-card-title" style={{ marginBottom: 12 }}>
          Neutral ramp
        </h4>
        <TokenSwatchGrid ramp="neutral" />
      </div>

      <div>
        <h4 className="hx-docs-card-title" style={{ marginBottom: 12 }}>
          Status ramps
        </h4>
        <TokenSwatchGrid ramp="success" />
        <TokenSwatchGrid ramp="warning" />
        <TokenSwatchGrid ramp="error" />
        <TokenSwatchGrid ramp="info" />
      </div>
    </div>

  </details>
</section>

</div>
