{/* Accessibility / Success Criteria.mdx — WCAG SC ledger + 7-gate release quality bar */}

import { Meta } from '@storybook/addon-docs/blocks';
import { EyebrowHeading, SectionHead, StatCard, DocsCard } from '../_components';

<Meta
  title="Accessibility/Success Criteria"
  parameters={{ controls: { disable: true }, actions: { disable: true } }}
/>

<div className="hx-docs">

<EyebrowHeading
  eyebrow="Accessibility · Conformance bar"
  title="How we measure conformance — the success criteria + the release gate."
  lede={
    <>
      Two ledgers, one bar. The <strong>WCAG success criteria</strong> below enumerate the rows
      HELiX commits to across Level A, AA, and the AAA criteria the formal harness measures on the
      P0 surface (per <code>aaa-verdicts.json</code>: 44 components, 376 Supports, 109 Not
      Applicable, 0 Partial, 0 Fail across 11 measured criteria). The{' '}
      <strong>release quality bar</strong> further down lists the build-time gates that block any
      merge below the floor. A criterion without a test is a marketing claim — every row cites its
      method.
    </>
  }
/>

<section>
  <SectionHead title="At a glance" meta="WCAG 2.2 · evidence-backed · self-cert" />
  <div className="hx-docs-stats">
    <StatCard num="11" label="AAA cert criteria" sub="measured by aaa-formal-audit.mjs" />
    <StatCard num="44" label="P0 components" sub="376 Supports · 109 NA · 0 Partial" />
    <StatCard num="0" unit=" Fail" label="Cert failures" sub="aaa-verdicts.json" />
    <StatCard num="2.2" label="WCAG version" sub="formal AAA harness baseline" />
  </div>
</section>

<section>
  <SectionHead
    title="WCAG success criteria — the ledger"
    meta="A & AA committed across all components · AAA on P0"
  />
  <p
    style={{
      color: 'var(--hx-color-text-secondary)',
      maxWidth: '70ch',
      marginBottom: 16,
      fontSize: 14,
      lineHeight: 1.6,
    }}
  >
    Each row pairs a WCAG success criterion with the test that proves it. The A and AA rows below
    are author-tracked engineering targets (status reflects the most common evidence path); they
    are NOT the formal AAA cert verdicts. The canonical conformance authority is the formal harness
    output in <code>aaa-verdicts.json</code> — refer to{' '}
    <a href="/?path=/docs/accessibility-vpat-2-5--docs">VPAT 2.5</a> for the row-by-row Supports /
    Not Applicable / Partial / Fail verdicts. Where this ledger and the VPAT disagree, the VPAT
    wins.
  </p>

<div className="hx-docs-sc-table">
  {[
    ['1.1.1', 'Non-text content', 'A', 'pass', 'axe-core: image-alt, role-img-alt'],
    ['1.3.1', 'Info & relationships', 'A', 'pass', 'axe-core: aria-* + landmarks'],
    ['1.3.2', 'Meaningful sequence', 'A', 'pass', 'play(): tab-order assertion'],
    ['1.4.1', 'Use of color', 'A', 'pass', 'manual + design-token review'],
    ['1.4.3', 'Contrast (minimum) ≥ 4.5:1', 'AA', 'pass', 'contrast-report.json (CI)'],
    ['1.4.4', 'Resize text to 200%', 'AA', 'pass', 'rem-only typography · VRT'],
    ['1.4.6', 'Contrast (enhanced) ≥ 7:1', 'AAA', 'pass', 'aaa-formal-audit.mjs (P0)'],
    ['1.4.10', 'Reflow at 320px', 'AA', 'pass', 'VRT viewport matrix'],
    ['1.4.11', 'Non-text contrast ≥ 3:1', 'AA', 'pass', 'contrast-report.json (UI)'],
    ['1.4.12', 'Text spacing', 'AA', 'pass', 'logical-property author rules'],
    [
      '1.4.13',
      'Content on hover/focus',
      'AA',
      'pass',
      'play(): dismissible · persistent · hoverable',
    ],
    ['2.1.1', 'Keyboard', 'A', 'pass', 'play(): keydown matrix per pattern'],
    ['2.1.2', 'No keyboard trap', 'A', 'pass', 'play(): tab cycles + escape'],
    ['2.4.3', 'Focus order', 'A', 'pass', 'play(): expected sequence'],
    ['2.4.7', 'Focus visible', 'AA', 'pass', 'VRT :focus-visible specimens'],
    ['2.5.5', 'Target size (enhanced) ≥ 44×44', 'AAA', 'pass', 'CSS min-height + VRT'],
    ['3.3.1', 'Error identification', 'A', 'pass', 'aria-invalid + aria-describedby'],
    [
      '3.3.3',
      'Error suggestion',
      'AA',
      'pass',
      'doc-required field + axe label-content-name-mismatch',
    ],
    ['3.3.4', 'Error prevention (legal)', 'AA', 'manual', 'destructive-confirm pattern review'],
    [
      '4.1.2',
      'Name, role, value',
      'A',
      'pass',
      'axe-core: aria-required-attr · aria-valid-attr-value',
    ],
    [
      '4.1.3',
      'Status messages',
      'AA',
      'pass',
      'live-region pattern · play() reader-text assertion',
    ],
  ].map(([num, name, grade, status, method]) => (
    <div key={num} className="hx-docs-sc-row" data-status={status}>
      <span className="hx-docs-sc-num">{num}</span>
      <span className="hx-docs-sc-name">
        <strong>{name}</strong>
      </span>
      <span className="hx-docs-sc-grade" data-status={status}>
        {grade}
      </span>
      <span className="hx-docs-sc-method">{method}</span>
    </div>
  ))}
</div>

  <p
    style={{
      marginTop: 16,
      fontSize: 12,
      color: 'var(--hx-color-text-muted)',
      fontFamily: 'var(--hx-font-family-mono)',
      lineHeight: 1.6,
    }}
  >
    Methods key: <strong>axe-core</strong> = rule run in the per-story a11y addon; <strong>play()</strong>{' '}
    = Storybook interaction test (Vitest browser mode); <strong>VRT</strong> = visual regression
    snapshot; <strong>contrast-report.json</strong> = packages/hx-tokens contrast scan; <strong>manual</strong>{' '}
    = expert review, no automated rule covers this SC reliably. Per{' '}
    <a href="https://www.deque.com/automated-accessibility-coverage-report/">Deque's automated-coverage research</a>
    {/* source: https://www.deque.com/automated-accessibility-coverage-report/ (verified 2026-05-07) */}
    , axe-core surfaces ~57% of accessibility issues found in real-world audits — but only the
    subset of WCAG SCs whose violations have a deterministic machine signature.
  </p>
</section>

<section>
  <SectionHead
    title="Release quality bar — seven gates"
    meta="all green · or no merge · enforced in CI"
  />
  <p
    style={{
      color: 'var(--hx-color-text-secondary)',
      maxWidth: '70ch',
      marginBottom: 24,
      fontSize: 14,
      lineHeight: 1.6,
    }}
  >
    The success criteria above describe the goal; these seven gates describe how every PR is
    measured against it. The pipeline blocks merge on any failure — there is no override path
    for accessibility regressions.
  </p>

  <div
    style={{
      display: 'grid',
      gridTemplateColumns: 'repeat(auto-fit, minmax(260px, 1fr))',
      gap: 16,
    }}
  >
    {[
      {
        n: 1,
        name: 'TypeScript strict',
        cmd: 'pnpm run type-check',
        threshold: 'Zero errors. No `any`. No `@ts-ignore`.',
        kind: 'automated',
      },
      {
        n: 2,
        name: 'Test suite',
        cmd: 'pnpm run test',
        threshold: '100% pass · ≥80% coverage on changed surface',
        kind: 'automated',
      },
      {
        n: 3,
        name: 'Accessibility',
        cmd: 'axe-core (per-story) + play() assertions',
        threshold: 'Zero critical · zero serious axe violations',
        kind: 'automated',
      },
      {
        n: 4,
        name: 'Storybook coverage',
        cmd: 'pnpm --filter=storybook run build',
        threshold: 'Stories build clean; every variant + state has a story',
        kind: 'review',
      },
      {
        n: 5,
        name: 'CEM accuracy',
        cmd: 'pnpm run cem',
        threshold: 'Manifest matches actual public API',
        kind: 'automated',
      },
      {
        n: 6,
        name: 'Bundle size',
        cmd: 'pnpm check:bundle (per-component size scan)',
        threshold:
          'CI ceiling 16 KB / component, 200 KB total (bundle-budgets.json); aspirational floor 5 KB / 50 KB (.bundle-budget.json) for atoms',
        kind: 'automated',
      },
      {
        n: 7,
        name: 'Code review',
        cmd: '3-tier review · code-reviewer → senior → chief',
        threshold: 'All tiers approved + CodeRabbit green',
        kind: 'review',
      },
    ].map((gate) => (
      <article
        key={gate.n}
        style={{
          background: 'var(--hx-color-surface-raised)',
          border: '1px solid var(--hx-color-border-default)',
          borderRadius: 14,
          padding: 20,
          display: 'flex',
          flexDirection: 'column',
          gap: 12,
          borderTop:
            gate.kind === 'automated'
              ? '3px solid var(--hx-color-success-500)'
              : '3px solid var(--hx-color-info-500)',
        }}
      >
        <div style={{ display: 'flex', alignItems: 'center', gap: 12 }}>
          <span
            style={{
              width: 32,
              height: 32,
              borderRadius: '50%',
              background: 'var(--hx-color-text-primary)',
              color: 'var(--hx-color-surface-default)',
              display: 'grid',
              placeItems: 'center',
              fontFamily: 'var(--hx-font-family-mono)',
              fontWeight: 700,
              fontSize: 13,
              flexShrink: 0,
            }}
          >
            {gate.n}
          </span>
          <strong style={{ fontSize: 15, color: 'var(--hx-color-text-primary)' }}>{gate.name}</strong>
          <span
            style={{
              marginLeft: 'auto',
              fontFamily: 'var(--hx-font-family-mono)',
              fontSize: 9,
              textTransform: 'uppercase',
              letterSpacing: '0.08em',
              color: 'var(--hx-color-text-muted)',
            }}
          >
            {gate.kind}
          </span>
        </div>
        <code
          style={{
            fontFamily: 'var(--hx-font-family-mono)',
            fontSize: 12,
            color: 'var(--hx-color-text-link)',
            background: 'var(--hx-color-surface-sunken)',
            padding: '4px 8px',
            borderRadius: 6,
            display: 'inline-block',
            alignSelf: 'flex-start',
            wordBreak: 'break-word',
          }}
        >
          {gate.cmd}
        </code>
        <p
          style={{
            margin: 0,
            fontSize: 13,
            color: 'var(--hx-color-text-secondary)',
            lineHeight: 1.5,
          }}
        >
          {gate.threshold}
        </p>
      </article>
    ))}
  </div>
</section>

<section>
  <SectionHead title="What we do not claim — yet" meta="honest gaps · upgrade path documented" />
  <div
    style={{
      display: 'grid',
      gridTemplateColumns: 'repeat(auto-fit, minmax(280px, 1fr))',
      gap: 16,
    }}
  >
    <DocsCard title="WCAG 2.2" tag="self-cert baseline — formal harness">
      <p style={{ margin: 0, fontSize: 13, color: 'var(--hx-color-text-secondary)' }}>
        HELiX self-certs WCAG&nbsp;2.2 Level&nbsp;AAA on its P0 surface via the formal harness (
        <code>scripts/aaa-formal-audit.mjs</code>, baseline standard{' '}
        <a href="https://www.w3.org/TR/WCAG22/">WCAG 2.2 Recommendation</a>). 2.2 SCs the harness
        explicitly measures: 1.4.6 (Contrast Enhanced), 1.4.9 (Images of Text), 2.1.3 (Keyboard No
        Exception), 2.3.3 (Animation from Interactions), 2.4.12 (Focus Not Obscured Enhanced),
        2.4.13 (Focus Appearance), 2.5.5 (Target Size Enhanced), 3.2.5 (Change on Request), 3.3.6
        (Error Prevention All). Plus 2 peer standards (forced-colors, APG keyboard).{' '}
        <a href="/?path=/docs/accessibility-self-cert-scope--docs">
          What the cert covers and doesn&apos;t
        </a>
        .
      </p>
    </DocsCard>
    <DocsCard title="Cognitive AAA SCs" tag="3.1.5 · 3.1.6 · scope-limited">
      <p style={{ margin: 0, fontSize: 13, color: 'var(--hx-color-text-secondary)' }}>
        Reading-level (3.1.5) and Pronunciation (3.1.6) are content-side SCs. HELiX provides the
        rendering primitives (semantic <code>hx-text</code>, <code>hx-prose</code>); the consuming
        app owns the plain-language content and any pronunciation hints (e.g. native HTML{' '}
        <code>&lt;ruby&gt;</code>) that fulfill them. <code>hx-text</code> does not expose a
        ruby-specific API.
      </p>
    </DocsCard>
    <DocsCard title="VPAT 2.5" tag="published v1.0 · refreshed per release">
      <p style={{ margin: 0, fontSize: 13, color: 'var(--hx-color-text-secondary)' }}>
        The formal <a href="https://www.itic.org/policy/accessibility/vpat">VPAT&nbsp;2.5</a>
        {/* source: https://www.itic.org/policy/accessibility/vpat (verified 2026-05-07) */} (ITI
        ICT Accessibility Testing Initiative template) for <code>@helixui/library</code> is
        published at v1.0 and refreshes with every release. The SC ledger above is the engineering
        view; the VPAT is the row-by-row Supports / Not Applicable / Partial / Fail report.
      </p>
    </DocsCard>
  </div>
</section>

<section>
  <SectionHead title="Cross-references" meta="related deep-dives" />
  <ul
    style={{
      margin: 0,
      paddingLeft: 20,
      fontSize: 14,
      color: 'var(--hx-color-text-secondary)',
      lineHeight: 1.8,
    }}
  >
    <li>
      <a href="/?path=/docs/accessibility-dashboard--docs">
        ← Accessibility · Dashboard (overview + headline numbers)
      </a>
    </li>
    <li>
      <a href="/?path=/docs/accessibility-consumer-obligations--docs">
        Consumer Obligations (partial-AAA SC contracts: 1.3.6, 1.4.8, 2.2.4, 2.2.6, 2.4.10, 3.1.4,
        3.3.5)
      </a>
    </li>
    <li>
      <a href="/?path=/docs/accessibility-contrast-deep-dive--docs">
        Contrast Deep-Dive (per-mode pair table)
      </a>
    </li>
    <li>
      <a href="/?path=/docs/accessibility-keyboard-contracts--docs">
        Keyboard Contracts (per-pattern keydown maps)
      </a>
    </li>
    <li>
      <a href="/?path=/docs/accessibility-focus-management--docs">
        Focus Management (trap · restore · roving tabindex)
      </a>
    </li>
    <li>
      <a href="/?path=/docs/accessibility-forced-colors--docs">
        Forced Colors (Windows HC + system keywords)
      </a>
    </li>
  </ul>
</section>

</div>
