---
/**
 * ComponentLoader — client-side script that imports @helixui/library
 * so custom elements register in the browser.
 * Include on any page that renders live web component demos.
 *
 * Uses the main barrel import so ALL components register at once.
 * This is fine for docs — tree-shaking isn't needed here.
 */
---

<script>
  import '@helixui/library';
</script>
