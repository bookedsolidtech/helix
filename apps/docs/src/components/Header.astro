---
import config from 'virtual:starlight/user-config';
import LanguageSelect from 'virtual:starlight/components/LanguageSelect';
import Search from 'virtual:starlight/components/Search';
import SiteTitle from 'virtual:starlight/components/SiteTitle';
import SocialIcons from 'virtual:starlight/components/SocialIcons';
import ThemeSelect from 'virtual:starlight/components/ThemeSelect';

const shouldRenderSearch =
  config.pagefind || config.components.Search !== '@astrojs/starlight/components/Search.astro';

const showcasePages = [
  {
    href: '/enterprise-architecture/',
    icon: 'fa-solid fa-building',
    title: 'Enterprise Architecture',
    desc: 'TypeScript-first, CEM-driven, token-powered',
    colorClass: 'sc-cyan',
    color: '#06b6d4',
    tag: 'Architecture',
  },
  {
    href: '/tech-stack/',
    icon: 'fa-solid fa-layer-group',
    title: 'The 2026 Stack',
    desc: 'Lit 3, Vite 7, Storybook 10, Web Test Runner',
    colorClass: 'sc-accent',
    color: '#8b5cf6',
    tag: 'Tooling',
  },
  {
    href: '/system-architecture/',
    icon: 'fa-brands fa-drupal',
    title: 'System Architecture',
    desc: 'CEM-to-Drupal pipeline, TWIG templates, SDC',
    colorClass: 'sc-amber',
    color: '#f59e0b',
    tag: 'Integration',
  },
  {
    href: '/healthcare-accessibility/',
    icon: 'fa-solid fa-universal-access',
    title: 'Healthcare & A11y',
    desc: 'WCAG 2.2 AA, Section 508, HIPAA-ready',
    colorClass: 'sc-green',
    color: '#10b981',
    tag: 'Compliance',
  },
  {
    href: '/developer-experience/',
    icon: 'fa-solid fa-rocket',
    title: 'Developer Experience',
    desc: 'Quality pipeline & DX',
    colorClass: 'sc-electric',
    color: '#3b82f6',
    tag: 'DX',
  },
  {
    href: '/platform-roadmap/',
    icon: 'fa-solid fa-chart-line',
    title: 'Platform Roadmap',
    desc: '89 issues from 13 agent reviews',
    colorClass: 'sc-red',
    color: '#ef4444',
    tag: 'Health',
  },
];

const currentPath = Astro.url.pathname;
---

<div class="header sl-flex">
  <div class="title-wrapper sl-flex">
    <SiteTitle />
  </div>

  <wc-showcase-dropdown class="showcase-dropdown print:hidden">
    <button
      class="showcase-trigger"
      aria-expanded="false"
      aria-haspopup="true"
      aria-label="Showcase pages"
    >
      <span class="trigger-icon-wrap">
        <i class="fa-solid fa-compass" aria-hidden="true"></i>
      </span>
      <span class="showcase-trigger-text">Showcases</span>
      <i class="fa-solid fa-chevron-down showcase-chevron" aria-hidden="true"></i>
    </button>

    <div class="showcase-panel" role="menu" aria-label="Showcase pages">
      <!-- Molecular particle canvas -->
      <canvas class="panel-particles" aria-hidden="true"></canvas>

      <div class="mega-layout">
        <!-- Left column: showcase cards -->
        <div class="mega-main">
          <div class="mega-main-header">
            <span class="showcase-panel-badge">
              <i class="fa-solid fa-star" aria-hidden="true"></i>
              Visual Experiences
            </span>
          </div>
          <div class="mega-grid">
            {showcasePages.map((page, index) => (
              <a
                href={page.href}
                class:list={['showcase-item', page.colorClass, { 'is-active': currentPath.startsWith(page.href) }]}
                role="menuitem"
                style={`--item-index: ${index}; --item-color: ${page.color};`}
              >
                <div class="showcase-item-icon">
                  <i class={page.icon} aria-hidden="true"></i>
                  <div class="icon-glow" aria-hidden="true"></div>
                </div>
                <div class="showcase-item-text">
                  <div class="showcase-item-title-row">
                    <span class="showcase-item-title">{page.title}</span>
                    <span class="showcase-item-tag">{page.tag}</span>
                  </div>
                  <div class="showcase-item-desc">{page.desc}</div>
                </div>
                <div class="showcase-item-arrow-wrap">
                  <i class="fa-solid fa-arrow-right showcase-item-arrow" aria-hidden="true"></i>
                </div>
                <div class="item-accent-line" aria-hidden="true"></div>
              </a>
            ))}
          </div>
        </div>

        <!-- Right column: Component Galaxy CTA -->
        <div class="mega-sidebar">
          <div class="sidebar-content">
            <div class="sidebar-visual" aria-hidden="true">
              <div class="visual-ring ring-1"></div>
              <div class="visual-ring ring-2"></div>
              <div class="visual-ring ring-3"></div>
              <div class="visual-core"></div>
            </div>

            <div class="sidebar-headline">Component Galaxy</div>
            <p class="sidebar-desc">
              Every component, live and interactive. Search, filter by category, and explore the full design system on one page.
            </p>

            <div class="sidebar-stats">
              <div class="stat-item">
                <span class="stat-value">13</span>
                <span class="stat-label">Components</span>
              </div>
              <div class="stat-divider" aria-hidden="true"></div>
              <div class="stat-item">
                <span class="stat-value">Live</span>
                <span class="stat-label">Demos</span>
              </div>
              <div class="stat-divider" aria-hidden="true"></div>
              <div class="stat-item">
                <span class="stat-value">CEM</span>
                <span class="stat-label">Driven</span>
              </div>
            </div>

            <a href="/component-gallery/" class="sidebar-cta">
              <span>Open Gallery</span>
              <i class="fa-solid fa-arrow-right" aria-hidden="true"></i>
            </a>
          </div>
        </div>
      </div>
    </div>
  </wc-showcase-dropdown>

  <div class="sl-flex right-group print:hidden">
    {shouldRenderSearch && <Search />}
    <div class="sl-hidden md:sl-flex right-inner">
      <div class="sl-flex social-icons">
        <SocialIcons />
      </div>
      <ThemeSelect />
      <LanguageSelect />
    </div>
    <button class="logout-btn" type="button" aria-label="Lock site" title="Lock site">
      <i class="fa-solid fa-right-from-bracket" aria-hidden="true"></i>
    </button>
  </div>
</div>

<script>
  import '../scripts/wc-showcase-dropdown.ts';
</script>

<style>
  /* ==========================================
     HEADER LAYOUT
     ========================================== */
  .header {
    display: flex !important;
    align-items: center;
    gap: var(--sl-nav-gap);
    height: 100%;
  }

  .title-wrapper {
    overflow: clip;
    padding: 0.25rem;
    margin: -0.25rem;
    min-width: 0;
    flex-shrink: 0;
  }

  .right-group {
    margin-left: auto;
    gap: 1rem;
    align-items: center;
    flex-shrink: 0;
  }

  .logout-btn {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 28px;
    height: 28px;
    border: none;
    border-radius: 6px;
    background: transparent;
    color: var(--sl-color-gray-4);
    font-size: 0.75rem;
    cursor: pointer;
    transition: all 0.2s ease;
    opacity: 0.4;
    flex-shrink: 0;
    /* Expand touch target to 44px minimum on touch devices */
    position: relative;
  }

  .logout-btn::after {
    content: '';
    position: absolute;
    inset: -8px;
  }

  .logout-btn:hover {
    opacity: 1;
    color: #ef4444;
    background: rgba(239, 68, 68, 0.08);
  }

  .right-inner {
    gap: 1rem;
    align-items: center;
  }

  .social-icons {
    gap: 1rem;
    align-items: center;
  }

  .social-icons::after {
    content: '';
    height: 2rem;
    border-inline-end: 1px solid var(--sl-color-gray-5);
  }

  /* ==========================================
     SHOWCASE DROPDOWN - TRIGGER
     ========================================== */
  .showcase-dropdown {
    position: relative;
    flex-shrink: 0;
  }

  .showcase-trigger {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.45rem 0.7rem;
    min-height: 44px;
    border: 1px solid rgba(255, 255, 255, 0.08);
    border-radius: 0.625rem;
    background: rgba(255, 255, 255, 0.03);
    color: var(--sl-color-gray-2);
    font-family: var(--sl-font);
    font-size: 0.8125rem;
    font-weight: 600;
    cursor: pointer;
    transition: all 0.3s cubic-bezier(0.22, 1, 0.36, 1);
    white-space: nowrap;
    line-height: 1;
    position: relative;
  }

  .trigger-icon-wrap {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 22px;
    height: 22px;
    border-radius: 6px;
    background: linear-gradient(135deg, rgba(102, 126, 234, 0.15), rgba(139, 92, 246, 0.15));
    font-size: 0.6875rem;
    transition: all 0.3s cubic-bezier(0.22, 1, 0.36, 1);
  }

  .showcase-trigger:hover {
    background: rgba(255, 255, 255, 0.06);
    border-color: rgba(255, 255, 255, 0.12);
    color: var(--sl-color-white);
    box-shadow: 0 4px 20px rgba(0, 0, 0, 0.2);
  }

  .showcase-trigger:hover .trigger-icon-wrap {
    background: linear-gradient(135deg, rgba(102, 126, 234, 0.25), rgba(139, 92, 246, 0.25));
    box-shadow: 0 0 12px rgba(102, 126, 234, 0.2);
  }

  .showcase-trigger[aria-expanded='true'] {
    background: rgba(102, 126, 234, 0.08);
    border-color: rgba(102, 126, 234, 0.3);
    color: var(--sl-color-white);
    box-shadow:
      0 0 0 1px rgba(102, 126, 234, 0.2),
      0 4px 24px rgba(102, 126, 234, 0.15);
  }

  .showcase-trigger[aria-expanded='true'] .trigger-icon-wrap {
    background: linear-gradient(135deg, rgba(102, 126, 234, 0.3), rgba(139, 92, 246, 0.3));
    box-shadow: 0 0 16px rgba(102, 126, 234, 0.3);
  }

  .showcase-trigger:focus-visible {
    outline: 3px solid var(--sl-color-accent);
    outline-offset: 2px;
  }

  .showcase-trigger-text {
    display: none;
  }

  .showcase-chevron {
    font-size: 0.5625rem;
    transition: transform 0.3s cubic-bezier(0.22, 1, 0.36, 1);
    display: none;
    opacity: 0.6;
  }

  .showcase-trigger[aria-expanded='true'] .showcase-chevron {
    transform: rotate(180deg);
  }

  @media (min-width: 50rem) {
    .showcase-trigger {
      padding: 0.45rem 0.85rem;
    }
    .showcase-trigger-text {
      display: inline;
    }
    .showcase-chevron {
      display: inline;
    }
  }

  /* ==========================================
     SHOWCASE DROPDOWN - MEGA PANEL
     ========================================== */
  .showcase-panel {
    display: none;
    flex-direction: column;
    position: absolute;
    top: calc(100% + 0.75rem);
    left: 0;
    width: 720px;
    max-width: calc(100vw - 2rem);
    padding: 0;
    background: rgba(10, 10, 24, 0.98);
    backdrop-filter: blur(40px) saturate(200%);
    -webkit-backdrop-filter: blur(40px) saturate(200%);
    border: 1px solid rgba(255, 255, 255, 0.06);
    border-radius: 1rem;
    box-shadow:
      0 24px 80px rgba(0, 0, 0, 0.55),
      0 8px 32px rgba(0, 0, 0, 0.3),
      inset 0 1px 0 rgba(255, 255, 255, 0.04);
    z-index: 200;
    opacity: 0;
    transform: translateY(-12px) scale(0.97);
    transition:
      opacity 0.3s cubic-bezier(0.22, 1, 0.36, 1),
      transform 0.3s cubic-bezier(0.22, 1, 0.36, 1);
    overflow: hidden;
  }

  .showcase-panel.is-open {
    opacity: 1;
    transform: translateY(0) scale(1);
  }

  /* Molecular particle canvas */
  .panel-particles {
    position: absolute;
    inset: 0;
    border-radius: inherit;
    pointer-events: none;
    z-index: 0;
    opacity: 0;
    transition: opacity 0.6s ease 0.15s;
  }

  .showcase-panel.is-open .panel-particles {
    opacity: 0.7;
  }

  /* Mobile: full-width fixed panel */
  @media (max-width: 49.99rem) {
    .showcase-panel {
      position: fixed;
      top: var(--sl-nav-height);
      left: 0.5rem;
      right: 0.5rem;
      width: auto;
      max-width: none;
      max-height: calc(100vh - var(--sl-nav-height) - 1rem);
      overflow-y: auto;
      transform: translateY(-12px) scale(0.98);
      border-top-left-radius: 0;
      border-top-right-radius: 0;
    }
    .showcase-panel.is-open {
      transform: translateY(0) scale(1);
    }
  }

  /* ==========================================
     MEGA LAYOUT
     ========================================== */
  .mega-layout {
    display: flex;
    position: relative;
    z-index: 1;
  }

  .mega-main {
    flex: 1;
    min-width: 0;
    padding: 0.75rem;
  }

  .mega-sidebar {
    width: 240px;
    flex-shrink: 0;
    border-left: 1px solid rgba(255, 255, 255, 0.06);
    display: flex;
    flex-direction: column;
  }

  @media (max-width: 49.99rem) {
    .mega-layout {
      flex-direction: column;
    }
    .mega-sidebar {
      width: 100%;
      border-left: none;
      border-top: 1px solid rgba(255, 255, 255, 0.06);
    }
  }

  /* ==========================================
     MAIN COLUMN HEADER
     ========================================== */
  .mega-main-header {
    padding: 0.25rem 0.5rem 0.5rem;
  }

  .showcase-panel-badge {
    display: inline-flex;
    align-items: center;
    gap: 0.375rem;
    font-size: 0.625rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    color: rgba(102, 126, 234, 0.7);
  }

  .showcase-panel-badge i {
    font-size: 0.5rem;
  }

  /* ==========================================
     MEGA GRID - SHOWCASE ITEMS
     ========================================== */
  .mega-grid {
    display: flex;
    flex-direction: column;
    gap: 2px;
  }

  .showcase-item {
    display: flex;
    align-items: center;
    gap: 0.875rem;
    padding: 0.75rem 0.75rem;
    border-radius: 0.75rem;
    text-decoration: none !important;
    color: inherit;
    transition:
      background 0.25s cubic-bezier(0.22, 1, 0.36, 1),
      border-color 0.25s cubic-bezier(0.22, 1, 0.36, 1),
      transform 0.25s cubic-bezier(0.22, 1, 0.36, 1),
      box-shadow 0.25s cubic-bezier(0.22, 1, 0.36, 1);
    border: 1px solid transparent;
    position: relative;
    overflow: hidden;
    /* Stagger animation */
    opacity: 0;
    transform: translateX(-8px);
  }

  .showcase-panel.is-open .showcase-item {
    opacity: 1;
    transform: translateX(0);
    transition:
      background 0.25s cubic-bezier(0.22, 1, 0.36, 1),
      border-color 0.25s cubic-bezier(0.22, 1, 0.36, 1),
      transform 0.4s cubic-bezier(0.22, 1, 0.36, 1),
      box-shadow 0.25s cubic-bezier(0.22, 1, 0.36, 1),
      opacity 0.4s cubic-bezier(0.22, 1, 0.36, 1);
    transition-delay: calc(var(--item-index, 0) * 50ms + 80ms);
  }

  .showcase-item:hover {
    background: rgba(255, 255, 255, 0.04);
    border-color: rgba(255, 255, 255, 0.08);
    transform: translateX(4px);
    box-shadow: 0 4px 20px rgba(0, 0, 0, 0.2);
  }

  .showcase-item:focus-visible {
    outline: 2px solid var(--sl-color-accent);
    outline-offset: -2px;
  }

  /* Active page indicator */
  .showcase-item.is-active {
    background: rgba(102, 126, 234, 0.06);
    border-color: rgba(102, 126, 234, 0.2);
    box-shadow:
      0 0 20px rgba(102, 126, 234, 0.08),
      inset 0 0 20px rgba(102, 126, 234, 0.03);
  }

  .showcase-item.is-active .item-accent-line {
    opacity: 1 !important;
    width: 3px !important;
  }

  /* Bottom accent line on hover */
  .item-accent-line {
    position: absolute;
    left: 0;
    top: 8px;
    bottom: 8px;
    width: 0;
    border-radius: 0 2px 2px 0;
    background: var(--item-color, #667eea);
    opacity: 0;
    transition: all 0.3s cubic-bezier(0.22, 1, 0.36, 1);
    box-shadow: 0 0 12px var(--item-color, #667eea);
  }

  .showcase-item:hover .item-accent-line {
    opacity: 0.8;
    width: 3px;
  }

  /* ==========================================
     SHOWCASE ITEM - ICON
     ========================================== */
  .showcase-item-icon {
    width: 48px;
    height: 48px;
    border-radius: 0.75rem;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 1.125rem;
    flex-shrink: 0;
    transition: all 0.3s cubic-bezier(0.22, 1, 0.36, 1);
    position: relative;
  }

  .icon-glow {
    position: absolute;
    inset: -4px;
    border-radius: inherit;
    background: radial-gradient(circle, var(--item-color, transparent) 0%, transparent 70%);
    opacity: 0;
    transition: opacity 0.3s ease;
    filter: blur(8px);
    pointer-events: none;
  }

  .showcase-item:hover .icon-glow {
    opacity: 0.25;
  }

  .showcase-item:hover .showcase-item-icon {
    transform: scale(1.08);
  }

  /* Color variants for icons */
  .sc-cyan .showcase-item-icon {
    color: #06b6d4;
    background: rgba(6, 182, 212, 0.08);
    border: 1px solid rgba(6, 182, 212, 0.15);
  }
  .sc-cyan:hover .showcase-item-icon {
    background: rgba(6, 182, 212, 0.12);
    border-color: rgba(6, 182, 212, 0.25);
    box-shadow: 0 0 20px rgba(6, 182, 212, 0.15);
  }

  .sc-accent .showcase-item-icon {
    color: hsl(250, 70%, 65%);
    background: rgba(139, 92, 246, 0.08);
    border: 1px solid rgba(139, 92, 246, 0.15);
  }
  .sc-accent:hover .showcase-item-icon {
    background: rgba(139, 92, 246, 0.12);
    border-color: rgba(139, 92, 246, 0.25);
    box-shadow: 0 0 20px rgba(139, 92, 246, 0.15);
  }

  .sc-amber .showcase-item-icon {
    color: #f59e0b;
    background: rgba(245, 158, 11, 0.08);
    border: 1px solid rgba(245, 158, 11, 0.15);
  }
  .sc-amber:hover .showcase-item-icon {
    background: rgba(245, 158, 11, 0.12);
    border-color: rgba(245, 158, 11, 0.25);
    box-shadow: 0 0 20px rgba(245, 158, 11, 0.15);
  }

  .sc-green .showcase-item-icon {
    color: #10b981;
    background: rgba(16, 185, 129, 0.08);
    border: 1px solid rgba(16, 185, 129, 0.15);
  }
  .sc-green:hover .showcase-item-icon {
    background: rgba(16, 185, 129, 0.12);
    border-color: rgba(16, 185, 129, 0.25);
    box-shadow: 0 0 20px rgba(16, 185, 129, 0.15);
  }

  .sc-electric .showcase-item-icon {
    color: #3b82f6;
    background: rgba(59, 130, 246, 0.08);
    border: 1px solid rgba(59, 130, 246, 0.15);
  }
  .sc-electric:hover .showcase-item-icon {
    background: rgba(59, 130, 246, 0.12);
    border-color: rgba(59, 130, 246, 0.25);
    box-shadow: 0 0 20px rgba(59, 130, 246, 0.15);
  }

  .sc-red .showcase-item-icon {
    color: #ef4444;
    background: rgba(239, 68, 68, 0.08);
    border: 1px solid rgba(239, 68, 68, 0.15);
  }
  .sc-red:hover .showcase-item-icon {
    background: rgba(239, 68, 68, 0.12);
    border-color: rgba(239, 68, 68, 0.25);
    box-shadow: 0 0 20px rgba(239, 68, 68, 0.15);
  }

  /* ==========================================
     SHOWCASE ITEM - TEXT
     ========================================== */
  .showcase-item-text {
    flex: 1;
    min-width: 0;
  }

  .showcase-item-title-row {
    display: flex;
    align-items: center;
    gap: 0.5rem;
  }

  .showcase-item-title {
    font-size: 0.875rem;
    font-weight: 700;
    color: var(--sl-color-white);
    line-height: 1.3;
    letter-spacing: -0.01em;
  }

  .showcase-item-tag {
    display: inline-flex;
    padding: 0.125rem 0.4rem;
    border-radius: 4px;
    font-size: 0.5625rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    color: var(--item-color, var(--sl-color-gray-3));
    background: color-mix(in srgb, var(--item-color, #667eea) 8%, transparent);
    border: 1px solid color-mix(in srgb, var(--item-color, #667eea) 15%, transparent);
    line-height: 1.4;
    white-space: nowrap;
    opacity: 0.8;
    transition: opacity 0.2s ease;
  }

  .showcase-item:hover .showcase-item-tag {
    opacity: 1;
  }

  .showcase-item-desc {
    font-size: 0.75rem;
    color: var(--sl-color-gray-3);
    line-height: 1.4;
    margin-top: 0.1875rem;
    opacity: 0.8;
  }

  .showcase-item:hover .showcase-item-desc {
    opacity: 1;
  }

  /* ==========================================
     SHOWCASE ITEM - ARROW
     ========================================== */
  .showcase-item-arrow-wrap {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 28px;
    height: 28px;
    border-radius: 8px;
    flex-shrink: 0;
    transition: all 0.25s cubic-bezier(0.22, 1, 0.36, 1);
    background: transparent;
  }

  .showcase-item:hover .showcase-item-arrow-wrap {
    background: rgba(255, 255, 255, 0.04);
  }

  .showcase-item-arrow {
    font-size: 0.6875rem;
    color: var(--sl-color-gray-4);
    opacity: 0;
    transform: translateX(-6px);
    transition: all 0.25s cubic-bezier(0.22, 1, 0.36, 1);
    flex-shrink: 0;
  }

  .showcase-item:hover .showcase-item-arrow {
    opacity: 1;
    transform: translateX(0);
    color: var(--item-color, var(--sl-color-gray-3));
  }

  /* ==========================================
     SIDEBAR / FEATURED AREA
     ========================================== */
  .sidebar-content {
    padding: 1.25rem;
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    gap: 0.875rem;
    height: 100%;
    justify-content: center;
    /* Stagger in with main items */
    opacity: 0;
    transform: translateY(8px);
    transition:
      opacity 0.4s cubic-bezier(0.22, 1, 0.36, 1),
      transform 0.4s cubic-bezier(0.22, 1, 0.36, 1);
    transition-delay: 200ms;
  }

  .showcase-panel.is-open .sidebar-content {
    opacity: 1;
    transform: translateY(0);
  }

  /* Animated rings visual */
  .sidebar-visual {
    position: relative;
    width: 72px;
    height: 72px;
    margin-bottom: 0.25rem;
  }

  .visual-ring {
    position: absolute;
    border-radius: 50%;
    border: 1px solid rgba(102, 126, 234, 0.15);
  }

  .ring-1 {
    inset: 0;
    animation: ring-pulse 3s ease-in-out infinite;
  }

  .ring-2 {
    inset: 8px;
    border-color: rgba(139, 92, 246, 0.2);
    animation: ring-pulse 3s ease-in-out infinite 0.5s;
  }

  .ring-3 {
    inset: 16px;
    border-color: rgba(6, 182, 212, 0.25);
    animation: ring-pulse 3s ease-in-out infinite 1s;
  }

  .visual-core {
    position: absolute;
    inset: 24px;
    border-radius: 50%;
    background: linear-gradient(135deg, rgba(102, 126, 234, 0.3), rgba(6, 182, 212, 0.3));
    box-shadow: 0 0 24px rgba(102, 126, 234, 0.2);
    animation: core-glow 4s ease-in-out infinite;
  }

  @keyframes ring-pulse {
    0%, 100% { transform: scale(1); opacity: 0.6; }
    50% { transform: scale(1.08); opacity: 1; }
  }

  @keyframes core-glow {
    0%, 100% { box-shadow: 0 0 24px rgba(102, 126, 234, 0.2); }
    50% { box-shadow: 0 0 32px rgba(102, 126, 234, 0.35), 0 0 48px rgba(6, 182, 212, 0.15); }
  }

  .sidebar-headline {
    font-size: 0.9375rem;
    font-weight: 700;
    color: var(--sl-color-white);
    letter-spacing: -0.02em;
    line-height: 1.2;
  }

  .sidebar-desc {
    font-size: 0.6875rem;
    color: var(--sl-color-gray-3);
    line-height: 1.5;
    margin: 0;
    opacity: 0.8;
  }

  /* Stats row */
  .sidebar-stats {
    display: flex;
    align-items: center;
    gap: 0.625rem;
    padding: 0.625rem 0;
    width: 100%;
  }

  .stat-item {
    display: flex;
    flex-direction: column;
    align-items: center;
    flex: 1;
    gap: 0.125rem;
  }

  .stat-value {
    font-size: 0.9375rem;
    font-weight: 800;
    color: var(--sl-color-white);
    font-family: var(--sl-font);
    letter-spacing: -0.02em;
    line-height: 1;
  }

  .stat-label {
    font-size: 0.5625rem;
    color: var(--sl-color-gray-4);
    text-transform: uppercase;
    letter-spacing: 0.06em;
    font-weight: 600;
    line-height: 1;
  }

  .stat-divider {
    width: 1px;
    height: 24px;
    background: rgba(255, 255, 255, 0.06);
    flex-shrink: 0;
  }

  /* CTA button */
  .sidebar-cta {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.5rem 1rem;
    border-radius: 0.5rem;
    font-size: 0.75rem;
    font-weight: 600;
    color: var(--sl-color-white);
    text-decoration: none !important;
    background: linear-gradient(135deg, rgba(102, 126, 234, 0.2), rgba(139, 92, 246, 0.2));
    border: 1px solid rgba(102, 126, 234, 0.2);
    transition: all 0.3s cubic-bezier(0.22, 1, 0.36, 1);
    width: 100%;
    justify-content: center;
  }

  .sidebar-cta:hover {
    background: linear-gradient(135deg, rgba(102, 126, 234, 0.3), rgba(139, 92, 246, 0.3));
    border-color: rgba(102, 126, 234, 0.35);
    box-shadow: 0 4px 20px rgba(102, 126, 234, 0.15);
    transform: translateY(-1px);
  }

  .sidebar-cta:focus-visible {
    outline: 2px solid var(--sl-color-accent);
    outline-offset: 2px;
  }

  .sidebar-cta i {
    font-size: 0.625rem;
    transition: transform 0.2s ease;
  }

  .sidebar-cta:hover i {
    transform: translateX(3px);
  }

  /* ==========================================
     LIGHT THEME ADJUSTMENTS
     ========================================== */
  :root:not([data-theme='dark']) .showcase-panel {
    background: rgba(248, 249, 252, 0.95);
    box-shadow:
      0 24px 80px rgba(0, 0, 0, 0.12),
      0 8px 32px rgba(0, 0, 0, 0.06),
      inset 0 1px 0 rgba(255, 255, 255, 0.8);
    border-color: rgba(0, 0, 0, 0.08);
  }


  :root:not([data-theme='dark']) .mega-sidebar {
    border-left-color: rgba(0, 0, 0, 0.06);
  }

  :root:not([data-theme='dark']) .showcase-item:hover {
    background: rgba(0, 0, 0, 0.03);
    border-color: rgba(0, 0, 0, 0.06);
  }

  :root:not([data-theme='dark']) .showcase-item.is-active {
    background: rgba(102, 126, 234, 0.05);
    border-color: rgba(102, 126, 234, 0.15);
  }

  :root:not([data-theme='dark']) .showcase-item-title {
    color: #1a1a2e;
  }

  :root:not([data-theme='dark']) .sidebar-headline {
    color: #1a1a2e;
  }

  :root:not([data-theme='dark']) .stat-value {
    color: #1a1a2e;
  }

  :root:not([data-theme='dark']) .stat-divider {
    background: rgba(0, 0, 0, 0.08);
  }

  :root:not([data-theme='dark']) .showcase-trigger {
    border-color: rgba(0, 0, 0, 0.1);
    background: rgba(0, 0, 0, 0.02);
    color: #4a4a6a;
  }

  :root:not([data-theme='dark']) .showcase-trigger:hover {
    background: rgba(0, 0, 0, 0.04);
    border-color: rgba(0, 0, 0, 0.15);
    color: #1a1a2e;
  }

  :root:not([data-theme='dark']) .showcase-trigger[aria-expanded='true'] {
    background: rgba(102, 126, 234, 0.06);
    border-color: rgba(102, 126, 234, 0.25);
  }

  :root:not([data-theme='dark']) .sidebar-cta {
    color: #1a1a2e;
  }

  :root:not([data-theme='dark']) .showcase-item-arrow-wrap {
    background: transparent;
  }

  :root:not([data-theme='dark']) .showcase-item:hover .showcase-item-arrow-wrap {
    background: rgba(0, 0, 0, 0.03);
  }

  /* ==========================================
     VIEWPORT CONTAINMENT
     ========================================== */
  @media (max-width: 800px) and (min-width: 50rem) {
    .showcase-panel {
      width: 600px;
    }
  }

  @media (min-width: 50rem) {
    .showcase-panel {
      left: 0;
    }
  }

  /* ==========================================
     REDUCED MOTION
     ========================================== */
  @media (prefers-reduced-motion: reduce) {
    .showcase-panel {
      transition: none;
    }
    .showcase-chevron {
      transition: none;
    }
    .showcase-item {
      transition: none;
      opacity: 1;
      transform: none;
    }
    .showcase-panel.is-open .showcase-item {
      transition: none;
      transition-delay: 0ms;
    }
    .showcase-item-icon,
    .showcase-item-arrow,
    .item-accent-line,
    .showcase-item-arrow-wrap,
    .sidebar-content,
    .sidebar-cta,
    .trigger-icon-wrap,
    .showcase-trigger {
      transition: none;
    }
    .panel-particles {
      animation: none;
    }
    .visual-ring,
    .visual-core {
      animation: none;
    }
  }
</style>
